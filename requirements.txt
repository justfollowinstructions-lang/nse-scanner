# NSE Long-Term Investment Scanner — Python dependencies
# Install: pip install -r requirements.txt

# ── Core data ──────────────────────────────────────────────────────────────────
yfinance>=0.2.50          # Yahoo Finance OHLCV + fundamentals
pandas>=2.1.0             # DataFrame operations
numpy>=1.26.0             # Numerical arrays / math
scipy>=1.11.0             # find_peaks, percentileofscore

# ── Web scraping (Screener.in fundamentals) ────────────────────────────────────
requests>=2.31.0          # HTTP requests for screener.in + NSE universe
beautifulsoup4>=4.12.2    # HTML parser for screener.in
lxml>=5.1.0               # Fast HTML/XML parser backend for BeautifulSoup

# ── Yahoo Finance 401 workaround on CI ────────────────────────────────────────
# Chrome impersonation prevents Yahoo from blocking GitHub Actions IPs
curl_cffi>=0.6.2          # curl with TLS fingerprinting (Chrome impersonation)

# ── Optional but recommended ──────────────────────────────────────────────────
python-dateutil>=2.8.2    # Robust date parsing
pytz>=2024.1              # Timezone support
